# 작업 티켓

이 문서는 Academy Follow-up을 단계별로 구현하기 위한 기능 단위 티켓 목록입니다.

원칙:

- 커밋은 가능한 한 티켓 단위로 나눕니다.
- 각 티켓은 독립적으로 리뷰 가능한 크기로 유지합니다.
- 파일럿 학원은 더배움프라임영수학원을 기준으로 합니다.
- MVP에서는 셀프서브 결제보다 파일럿 사용 가능성이 우선입니다.

## Phase 0: 프로젝트 기반

### T-001 프로젝트 초기화

상태: 완료

목표:
- Next.js 앱 초기화
- 기본 문서 생성
- GitHub repo 연결

완료 기준:
- `npm run build` 통과
- GitHub `main` 브랜치 push 완료

### T-002 제품/기술 기획 정리

상태: 완료

목표:
- 제품 기획서 정리
- 기술 설계서 정리
- 파일럿 학원 정보 반영

완료 기준:
- `docs/product-plan.md` 작성
- `docs/technical-plan.md` 작성

### T-003 단계별 티켓 문서 작성

상태: 완료

목표:
- 앞으로 구현할 작업을 Phase/Ticket 단위로 정리

완료 기준:
- `docs/tickets.md` 작성
- 다음 개발 티켓이 명확해야 함

## Phase 1: 멀티테넌트 기반

### T-101 플랫폼 관리자와 학원 권한 모델 정리

상태: 완료

목표:
- 슈퍼 관리자와 학원 내부 권한을 분리
- 파일럿 이후 여러 학원이 가입할 수 있는 권한 구조 확정

작업:
- `platform_admins` 테이블 추가
- `academies.owner_id` 추가
- `academies.plan`, `academies.status` 추가
- 권한 설명 문서화

완료 기준:
- DB 스키마에 플랫폼 관리자/학원 소유자 구조가 반영됨
- 기술 설계서에 권한 계층이 반영됨

### T-102 학원 워크스페이스 데이터 모델 보강

상태: 완료

목표:
- 학원별 브랜딩/설정/발신정보를 DB에서 관리할 수 있게 함

작업:
- `academies` 필드 점검
- `academy_settings` 분리 여부 결정
- 발신명/발신번호/문자 dry-run 설정 범위 결정

완료 기준:
- 학원별 설정 데이터가 코드 상수에 묶이지 않음
- 더배움프라임영수학원 seed 데이터 설계가 가능함

### T-103 Supabase 프로젝트 연결

상태: 완료

목표:
- 실제 Supabase 프로젝트와 로컬 앱 연결

작업:
- Supabase 프로젝트 생성: 완료
- `schema.sql` 적용: 완료
- migration 파일 추가: 완료
- 파일럿 seed 데이터 추가: 완료
- `.env.local` 설정: 완료
- Supabase client/server 연결 확인: 완료

완료 기준:
- 앱에서 Supabase 접속 가능
- 빌드 통과

메모:
- `GET /api/health/supabase`가 `{ "ok": true, "status": "connected" }`를 반환함
- 설정 절차는 `docs/supabase-setup.md` 참고

## Phase 2: 인증/온보딩

### T-201 로그인 화면 구현

상태: 완료

목표:
- 원장/선생님이 이메일로 로그인할 수 있게 함

작업:
- 로그인 페이지 생성: 완료
- Supabase Auth 연결: 완료
- 로그인 실패/성공 상태 처리: 완료
- 비로그인 사용자의 앱 화면 접근 차단: 완료

완료 기준:
- 로그인 후 앱 화면 접근 가능
- 비로그인 사용자는 로그인 화면으로 이동

메모:
- `/login`에서 Supabase 이메일/비밀번호 로그인을 처리합니다.
- `/app`은 서버에서 세션을 확인하고, 프로필과 학원 워크스페이스가 연결된 경우 운영 보드를 보여줍니다.
- 배포 환경에서 Supabase 환경변수가 빠진 경우 500 대신 설정 안내 상태를 보여줍니다.
- 실제 테스트 계정 연결은 T-203에서 진행합니다.

### T-202 학원 생성 플로우 구현

상태: 예정

목표:
- 가입 사용자가 자기 학원 워크스페이스를 만들 수 있게 함

작업:
- 학원명 입력
- slug 생성
- owner profile 생성
- 기본 문자 템플릿 생성

완료 기준:
- 새 사용자가 학원 워크스페이스를 생성할 수 있음

### T-203 파일럿 학원 seed 데이터 추가

상태: 예정

목표:
- 더배움프라임영수학원 파일럿을 바로 테스트할 수 있게 함

작업:
- 학원 데이터
- 선생님 계정 샘플
- 반 데이터
- 학생 샘플 데이터
- 기본 문자 템플릿

완료 기준:
- 로컬/개발 DB에서 파일럿 화면 확인 가능

## Phase 3: 팔로업 핵심 기능

### T-301 반/학생 목록 화면

상태: 예정

목표:
- 선생님이 담당 반과 학생 목록을 볼 수 있게 함

작업:
- 반 목록 조회
- 학생 목록 조회
- 담당 권한 필터 적용

완료 기준:
- DB 데이터 기반으로 반/학생 목록 표시

### T-302 문자 미리보기 생성

상태: 예정

목표:
- 학생과 사유를 선택하면 학원별 템플릿으로 문자 초안 생성

작업:
- `POST /api/messages/preview`
- 템플릿 변수 치환
- 본문 수정 가능 UI

완료 기준:
- 실제 발송 없이 문자 미리보기 생성 가능

### T-303 팔로업 생성 및 기록 저장

상태: 예정

목표:
- 발송 전/후 팔로업 업무 단위를 DB에 저장

작업:
- `followups` insert
- 상태값 `draft/sent/failed`
- 학생별 히스토리 조회

완료 기준:
- 학생별 팔로업 기록 확인 가능

### T-304 문자 발송 dry-run

상태: 예정

목표:
- 실제 문자 발송 없이 전체 발송 흐름을 검증

작업:
- `SMS_DRY_RUN=true` 처리
- `message_logs` 저장
- UI에 dry-run 표시

완료 기준:
- 실제 비용 없이 발송 플로우 테스트 가능

### T-305 SOLAPI 실제 발송

상태: 예정

목표:
- production 환경에서 실제 SMS/LMS 발송

작업:
- SOLAPI 키 설정
- 발신번호 설정
- 성공/실패 로그 저장

완료 기준:
- 테스트 번호로 실제 문자 수신 확인

## Phase 4: 원장 운영 보드

### T-401 오늘의 팔로업 대시보드

상태: 예정

목표:
- 원장이 오늘 발송/대기/실패 현황을 볼 수 있게 함

작업:
- 오늘 발송 수
- 사유별 수
- 반별 수
- 선생님별 수

완료 기준:
- 원장이 하루 운영 현황을 한 화면에서 확인 가능

### T-402 학생별 연락 히스토리

상태: 예정

목표:
- 학생별 과거 학부모 연락 기록을 볼 수 있게 함

작업:
- 학생 상세 화면
- 팔로업 타임라인
- 발송 상태 표시

완료 기준:
- 상담 전 학생별 연락 이력 확인 가능

### T-403 템플릿 관리

상태: 예정

목표:
- 원장이 학원 말투에 맞게 문자 템플릿을 수정할 수 있게 함

작업:
- 사유별 템플릿 목록
- 템플릿 수정
- 변수 가이드

완료 기준:
- DB 템플릿을 수정하면 미리보기/발송에 반영됨

## Phase 5: 파일럿 운영

### T-501 파일럿 반 2개 세팅

상태: 예정

목표:
- 더배움프라임영수학원에서 작은 범위로 실제 사용

작업:
- 반 2개 등록
- 학생 30명 내외 등록
- 선생님 2~3명 초대

완료 기준:
- 실제 수업 후 사용 가능

### T-502 2주 파일럿 지표 수집

상태: 예정

목표:
- 제품이 실제 운영 시간을 줄이는지 확인

측정:
- 하루 팔로업 생성 수
- 발송 전 문구 수정 비율
- 중복/오발송 여부
- 선생님 체감 속도
- 원장 대시보드 확인 빈도

완료 기준:
- 계속 만들지, 방향을 바꿀지 판단할 근거 확보
