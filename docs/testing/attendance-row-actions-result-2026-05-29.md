# 출석부 Row 액션 버튼 정리 결과 - 2026-05-29

## 범위

- T-442 출석부 학생 Row 액션 버튼 한 줄 정리
- 대상 파일: `src/app/app/attendance-board.tsx`
- DB/API/출석 저장 로직 변경 없음

## 변경 내용

- 학생 row 오른쪽 액션을 `도착 토글 -> 지각 -> 예외` 한 줄로 정리했습니다.
- `지각`은 원장 요구대로 빠른 체크 버튼으로 유지했습니다.
- `예외`는 결석/확인 필요/보강을 펼치는 버튼으로 유지했습니다.
- 예외 펼침 항목과 문자 연결 흐름은 기존 구조를 그대로 사용합니다.

## 확인 기준

- 모바일 390px에서 출석 액션이 같은 그룹으로 보입니다.
- 도착 토글은 `present/pending` 저장 흐름을 유지합니다.
- 지각 버튼은 `late` 저장과 결석/지각 문자 패널 연결을 유지합니다.
- 예외 버튼은 `결석 / 확인 필요 / 보강` 버튼을 펼칩니다.
